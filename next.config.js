/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add this line to disable TypeScript checking during build
  typescript: {
    ignoreBuildErrors: true,
  },
  // Your other config options...
}

module.exports = nextConfig 